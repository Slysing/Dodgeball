using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ButtonFlash : MonoBehaviour
{
    //[SerializeField] private GameObject m_pausedGameText;
   
    //void Update()
    //{
    //        m_pausedGameText.GetComponent<>();
    //        ColorBlock cb = currentButton.colors;
    //        cb.highlightedColor = Color.Lerp(Color.red, Color.blue, Mathf.PingPong(Time.time, 1));
    //        currentButton.colors = cb;
        
    //}
}
